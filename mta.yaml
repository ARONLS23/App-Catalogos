_schema-version: "3.2"
ID: tiles
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: tiles-destination-content
  type: com.sap.application.content
  requires:
  - name: tiles-destination-service
    parameters:
      content-target: true
  - name: tiles-repo-host
    parameters:
      service-key:
        name: tiles-repo-host-key
  - name: tiles-uaa
    parameters:
      service-key:
        name: tiles-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: tiles_html_repo_host
          ServiceInstanceName: tiles-html5-service
          ServiceKeyName: tiles-repo-host-key
          sap.cloud.service: tiles
        - Authentication: OAuth2UserTokenExchange
          Name: tiles_uaa
          ServiceInstanceName: tiles-xsuaa-service
          ServiceKeyName: tiles-uaa-key
          sap.cloud.service: tiles
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: tiles-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: tiles-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - tiles.zip
      name: tiles
      target-path: resources/
- name: tiles
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: tiles-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: tiles-destination-service
    service-plan: lite
- name: tiles-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: tiles-xsuaa-service
    service-plan: application
- name: tiles-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: tiles-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
